import { IAppConfig } from '../../interfaces/IAppConfig.js'

export const DefaultConfig: IAppConfig = {
  version: '0.0.0',
  description: 'A CLI application for interacting with an instance/installation of Uptime Kuma.',
  instanceUrl: 'https://uptime.4lch4.cloud',
  pushToken: 'DefaultPushTokenValue'
}
